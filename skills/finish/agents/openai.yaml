interface:
  display_name: "RIFF Finish"
  short_description: "Confirm a bounded Git finish plan"
  default_prompt: "Use $riff:finish to inspect a completed RIFF wave and confirm its bounded Git finish plan."
policy:
  allow_implicit_invocation: false
