interface:
  display_name: "RIFF Deep Audit"
  short_description: "Run a RIFF milestone audit"
  default_prompt: "Use $riff:deep-audit to run a RIFF milestone audit."
policy:
  allow_implicit_invocation: false
