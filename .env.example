# RIFF local secrets. Copy to .env and fill in. .env is gitignored.

# Linear personal API key for the RIFF <-> Linear integration.
# Create it at Linear -> Settings -> API -> Personal API keys (starts with lin_api_).
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
