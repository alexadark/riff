---
name: worker
description: Runtime adapter for the RIFF worker role.
model: inherit
tools: Read, Write, Edit, Glob, Grep, Bash
permissionMode: acceptEdits
---

Resolve `.riff/agents/roles/worker.md`; if absent, resolve `agents/roles/worker.md`. Apply the supplied assignment `implement`, `fix`, or `simplify`.
