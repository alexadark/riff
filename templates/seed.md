# Seed: {{TITLE}}

> Created: phase-{{N}} | By: {{AGENT_NAME}} | Deviation: R4
> Pre-approved: no

<!--
  Set `Pre-approved: yes (approved by <user>, <date>)` to skip the user-confirmation step
  when this seed's Trigger fires during /riff:next Step 2. The seed will be auto-integrated
  into the next eligible phase plan as a new task. Use sparingly — only for work the user
  has explicitly signed off on in a prior session.
-->

## Idea

{{What should be done and why}}

## Trigger

{{When this seed should be promoted to a ROADMAP phase. Be specific:}}
{{e.g. "After phase 5 (auth) is complete" or "When a second API provider is added" or "When the project has 10+ routes"}}

## Context

{{What was happening when this idea surfaced. Enough detail for a future planner to understand.}}
