# Gates Log — Phase {{PHASE_ID}}: {{PHASE_TITLE}}

> Phase branch: `riff/phase-{{PHASE_ID}}-{{PHASE_SLUG}}`
> Entries are appended by `/riff:next` as each gate resolves. One line per gate.

<!-- Step 4b — plan adversarial review (Codex) -->
<!-- Step 5b — simplifier -->
<!-- Step 5c — scope-checker -->
<!-- Step 5d — fallow audit (TS/JS only) -->
<!-- Step 6  — adversarial review (Codex) -->
<!-- Step 7b — improver -->

## Gate entries

<!-- Examples of what entries look like:
Step 4b: skipped — gate=false
Step 4b: skipped — trivial (< 3 tasks)
Step 4b: ran — model=gpt-5.5 effort=medium
Step 5b: skipped — auto: no refactor tags
Step 5c: MATCH
Step 5c: override — user accepted 1 dropped task after 3 cycles
Step 5d: skipped — not TS/JS
Step 5d: pass
Step 5d: warn — 2 findings
Step 5d: fail → fix-in-place cycle 1
Step 5d: accepted-exception — legacy duplication in vendor shim, not worth cleaning
Step 5d: override
Step 6: skipped — gate=false
Step 6: skipped — pure UI, low priority, diff < 100 lines
Step 6: ran — model=gpt-5.5 effort=medium
Step 7b: skipped — gate not met
Step 7b: ran
-->
